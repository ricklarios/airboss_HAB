const getAllPassengers = require('./getAllPassengers');
const getPassenger = require('./getPassenger');
const newPassenger = require('./newPassenger');
const editPassenger = require('./editPassenger');
const deletePassenger = require('./deletePassenger');

module.exports = {
    getAllPassengers,
    getPassenger,
    newPassenger,
    editPassenger,
    deletePassenger,
};
